import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Modern_Looking_Menu',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
